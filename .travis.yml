language: node_js

node_js:
  - "8"

env:
  global:
    - NODE_ENV=test
    - AUDIODEV=null

addons:
  apt:
    sources:
      - trusty-media
    packages:
      - ffmpeg
      - libasound2-dev

cache: yarn

script:
  - yarn install
  - yarn run lint:all
  - yarn run test:all
  - yarn run e2e -- -e chrome-headless